node_modules
.pnpm-store/
dist
# .diffing is deliberately NOT here — nothing should ever be written into the consumer repo. If you see one, delete it.
.playwright-mcp
# Rust TUI build artefacts (workspace lives in ./crates)
target/
# Docs site (Astro)
site/dist
site/.astro
site/node_modules
# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
/worktrees/

# npx skills install target (regenerated by ./skills.sh install)
/agent/
